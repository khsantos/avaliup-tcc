module.exports = {
  darkMode: 'class', // Certifica-se que o dark mode é controlado pela classe 'dark'
  theme: {
    extend: {
      colors: {
        // Defina suas cores personalizadas aqui, se necessário
        darkBackground: '#030712', // Exemplo de cor para o fundo no modo escuro
      },
    },
  },
  plugins: [],
}
